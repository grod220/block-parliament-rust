[build]
target = "index.html"
dist = "dist"

[watch]
watch = ["src", "index.html", "style"]

[serve]
address = "127.0.0.1"
port = 8080
open = false

# Tailwind hook disabled - using CDN for development
# [[hooks]]
# stage = "pre_build"
# command = "sh"
# command_arguments = ["-c", "npx tailwindcss -i style/main.css -o style/output.css --minify 2>/dev/null || true"]
